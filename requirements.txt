# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4

# Config
python-dotenv==1.0.1

# Vector DB (new SDK — matches the notebook's `from pinecone import Pinecone`)
pinecone==5.0.1

# Embeddings
sentence-transformers==2.7.0

# LangChain (versions pinned to avoid the langchain-core mismatch
# you hit in Colab — these are the last set known to work with langchain 0.1.x)
langchain==0.1.20
langchain-core==0.1.52
langchain-community==0.0.38
langchain-openai==0.1.7
